# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Real env values should never be committed — only .env.example (with
# placeholder values) is meant to be tracked. Set real values via your
# CI/CD platform's environment variables instead.
.env
.env.development
.env.production
.env.staging
.env.local
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
